-- ============================================================
-- v6.10 — 笔记图片支持
-- ============================================================
-- 说明：
--   笔记图片以 base64 data URL 形式内嵌在 notes.content 字段中
--   （markdown 语法：![alt](data:image/jpeg;base64,...)）
--   客户端在插入图片前会先压缩到最长边 1280px、JPEG 质量 0.75，
--   单张约 80~150KB，云端同步上限已从 100KB 提升到 1MB
--   （auth.ts 中的 MAX_CONTENT 常量，无需 DB 侧操作）。
--
-- 数据库侧：
--   notes.content 列在 schema.sql 中已经是 TEXT 类型（无长度限制），
--   无需修改表结构。本文件仅作为变更说明保留。
--
-- 影响：
--   - 单条笔记最多可包含 6~10 张压缩图片
--   - 超过 1MB 的笔记 content 会被客户端截断（保留前 1MB + 截断提示）
--   - 图片仅在客户端压缩，云端保存的就是压缩后的 base64
-- ============================================================

-- 无需执行任何 SQL，此文件仅作变更记录
SELECT 1;
